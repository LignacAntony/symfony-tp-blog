{% extends 'base.html.twig' %}

{% block title %}Language Details{% endblock %}

{% block body %}
    <div class="container mx-auto">
        <h1 class="text-3xl font-bold mb-6">Language Details</h1>

        <div class="bg-white shadow-md rounded-lg overflow-hidden mb-6">
            <table class="min-w-full divide-y divide-gray-200">
                <tbody class="divide-y divide-gray-200">
                    <tr>
                        <th class="px-6 py-3 bg-gray-50 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Code</th>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ language.code }}</td>
                    </tr>
                    <tr>
                        <th class="px-6 py-3 bg-gray-50 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Label</th>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ language.label }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="flex space-x-4">
            <a href="{{ path('app_language_index') }}" class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded transition duration-300 ease-in-out">
                Back to list
            </a>

            <a href="{{ path('app_language_edit', {'id': language.id}) }}" class="bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded transition duration-300 ease-in-out">
                Edit
            </a>

            {{ include('language/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}