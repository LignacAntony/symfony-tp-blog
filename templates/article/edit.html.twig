{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <div class="container mx-auto px-4 py-8">
        <h2 class="text-2xl font-bold mb-6">Edit Article</h1>
        <div class="bg-white shadow-md rounded-lg p-6 mb-6">
            {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="flex space-x-4 mb-6">
            <a href="{{ path('app_article_index') }}" class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded transition duration-300 ease-in-out">
                Back to list
            </a>
        </div>
        <div class="bg-white shadow-md rounded-lg p-6">
            {{ include('article/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}